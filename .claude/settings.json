{
  "_comment": "Repo-scoped Claude Code permissions. Merged into every session started with cwd inside this repo. Keep this narrow: only permissions specifically useful for working with this fork (installer runs, repo-relative test/build commands). Global CLIs (gh-axi, tasks-axi, chrome-devtools-axi, treehouse, no-mistakes, tmux, ~/.tmux-scripts) are allowlisted in ~/.claude/settings.json.",
  "permissions": {
    "allow": [
      "Bash(bash setup/install-noix.sh)",
      "Bash(bash setup/install-noix.sh:*)",
      "Bash(DRY_RUN=1 bash setup/install-noix.sh)",
      "Bash(chmod +x setup/*.sh)",
      "Bash(chmod +x files/tmux-scripts/*.sh)",
      "Bash(bash tests/mac_setup_test.sh)",
      "Bash(bash tests/*.sh)",
      "Bash(bash -n *)"
    ]
  }
}
